var AutoPager = new (function _AutoPager(){
/*
    $(function(){
        $(document).scroll(function(e){
            if($("#th-historyList").is(":visible")){
                var top = $(document).scrollTop();
                if(top == 0){
                    if(HistoryList.itemsReady){
                        HistoryList.flipPage(1/4, 5);
                    }
                    return;
                }
                var fullHeight = $(document).outerHeight();
                var screenHeight = $(window).height();
                var bottomSpace = fullHeight - screenHeight - top;
                if(fullHeight > screenHeight && bottomSpace == 0){
                    if(HistoryList.itemsReady){
                        HistoryList.flipPage(-1/4, 5);
                    }
                }
            }
        });
    });
*/
})();
